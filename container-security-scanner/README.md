# Container Security Scanner

Scans Docker configs: privileged mode, root user, host network, sensitive mounts, hardcoded secrets, dangerous capabilities, missing seccomp/AppArmor, vulnerable images.

```bash
python main.py
```

SOC L2 Analyst | github.com/demonchant
